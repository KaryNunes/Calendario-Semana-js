const dataAtual ? nova Date();
let diasDaSemana ["Domingo", "Segunda-feira", "Terça-feira", "Quarta-feira", "Quinta-feira", "Sexta-feira", "Sabado"];
const diaAtual ? dataAtual.getDay();

console.log("Dias da Semana:");
for (deixe i ? 0; i ? diasDaSemana.length; i++) ?
console.log(diasDaSemana[i]);
) )

console.log("Hoje é:", diasDaSemana[diaAtual]);
